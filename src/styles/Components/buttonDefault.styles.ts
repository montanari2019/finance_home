import { StyleSheet } from 'react-native';

export const styled = StyleSheet.create({
  button: {
    width: 175,
    height: 50,
    borderRadius: 15,
    justifyContent: 'center',
    alignItems: 'center',
    flexDirection: "row",
    gap: 16,
  },
});
